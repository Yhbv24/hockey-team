export class Member {
  constructor(public firstName: string, public lastName: string, public role: string, public position: string, public nationality: string, public age: number, public photo: string) {}
}
